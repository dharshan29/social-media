import { Box } from "@mui/material";
import React from "react";
import styles from "./index.style";

const Settings = () => {
	return <Box sx={styles.wrapperStyle}>Settings</Box>;
};

export default Settings;
